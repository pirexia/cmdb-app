// public/assets/js/app.js
// Tu JavaScript personalizado aquí
// Por ejemplo, para inicializar componentes de Bootstrap que no lo hacen automáticamente
$(document).ready(function() {
    // Ejemplo: Puedes añadir lógica para cerrar alertas automáticamente
    // window.setTimeout(function() {
    //     $(".alert").fadeTo(500, 0).slideUp(500, function(){
    //         $(this).remove();
    //     });
    // }, 4000); // 4 segundos
});
